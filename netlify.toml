[build]
  command = "npm run build"
  # @netlify/vite-plugin-tanstack-start: static assets + .netlify/v1/functions for SSR.
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"

[dev]
  command = "npm run dev"
  port = 8080
